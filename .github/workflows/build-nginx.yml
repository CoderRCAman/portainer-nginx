name: Build and Push Nginx Image to GHCR

on:
  push:
    branches: [ "master" ]

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repo
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Log in to GHCR
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCR_TOKEN }}

      # 3️⃣ Set image metadata (automatic tags & labels)
      - name: Set up Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/nginx-proxy
          tags: |
            type=raw,value=latest
            type=sha

      # 4️⃣ Build & push image to GHCR
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      # 5️⃣ Trigger Portainer stack redeploy (optional)
      # - name: Trigger Portainer redeploy
      #   if: success() && secrets.PORTAINER_WEBHOOK != ''
      #   run: |
      #     echo "Triggering Portainer redeploy..."
      #     curl --insecure -X POST ${{ secrets.PORTAINER_WEBHOOK }}
